---
wallos_docker_settings_default:
  network: "host"
  puid: "1000"
  pgid: "1000"
  tz: "Etc/UTC"
  published_ports:
    - "8282:80"

wallos_env_default:
  TZ: "{{ wallos_docker_settings.tz }}"
  PUID: "{{ wallos_docker_settings.puid }}"
  PGID: "{{ wallos_docker_settings.pgid }}"
